#!/bin/sh

case "$1" in
	"CMD_BOOT" | "CMD_START" | "CMD_RESTART")
        	
	/usr/sbin/sip_config SIP_CORE_Phone_CFGSync 99

		;;

	"CMD_STOP")

		;;

	*)
		echo $0 'Phone_CFGSync          - notify brnsip '
		;;
esac